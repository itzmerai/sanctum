{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Broader surface for the unlocked application window (KTD18). Plugin permissions are added by the unit that needs them: dialog for backup/CSV file pickers (U6), process for relaunch after Reset Vault (U6). Window show/hide is deliberately absent -- Rust drives those transitions (commands::windows), so a compromised WebView cannot reveal the app window on its own.",
  "local": true,
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "dialog:allow-save",
    "dialog:allow-open",
    "process:allow-restart"
  ]
}
